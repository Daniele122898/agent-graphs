# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Database (local state)
backend/db.sqlite
backend/db.sqlite-*
*.sqlite
*.sqlite-journal

# Node / frontend
node_modules/
frontend/dist/
frontend/.vite/
*.tsbuildinfo

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local configuration (holds API keys — config.example.yml is the committed shape)
config.yml

# OpenCode harness runtime artifacts: the harness stages ask_agent.ts here and
# OpenCode installs the tool's node_modules into it at run time. Generated, never
# source (the canonical tool lives in backend/harness/opencode/tools/).
.opencode/
